# Server-side key for the /api/deepseek proxy. Required — the route returns 500 without it.
# Local dev: copy to .env.local and fill in. Vercel: set it under Project Settings → Environment Variables.
DEEPSEEK_API_KEY=
