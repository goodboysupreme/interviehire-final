# Production (Vercel) — public config baked into the build at `next build`.
# These are NEXT_PUBLIC_ (not secrets). Vercel dashboard env vars, if set, take
# precedence over this file.
NEXT_PUBLIC_API_URL=https://interviehire-backend-production-ca93.up.railway.app/api
NEXT_PUBLIC_ENGINE_WEB_URL=https://interview.interviehire.com
